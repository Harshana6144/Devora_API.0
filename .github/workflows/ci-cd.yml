name: CI/CD FastAPI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build-deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repo
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Run tests (optional)
      run: echo "Tests passed!"

    - name: Deploy to server via SSH
      uses: appleboy/ssh-action@v0.1.7
      with:
        host: ${{ secrets.20.127.201.103 }}        # 20.127.201.103
        username: ${{ secrets.dpd }}  # dpd
        key: ${{ secrets.SERVER_SSH_KEY }}    # Private key stored in GitHub Secret
        port: ${{ secrets.22 }}      # Usually 22
        timeout: 60s                           # Increase timeout if needed
        script: |
          # Go to project directory
          cd /home/dpd/Devora_API.0

          # Activate virtual environment
          source venv/bin/activate

          # Pull latest code
          git pull origin main

          # Install dependencies
          pip install -r requirements.txt

          # Restart FastAPI app via PM2
          pm2 restart Devora_API || pm2 start venv/bin/uvicorn --name Devora_API --interpreter python3 -- api:app --host 0.0.0.0 --port 8000
          pm2 save

          # Restart Nginx
          sudo systemctl restart nginx
